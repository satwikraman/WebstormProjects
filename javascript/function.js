
function solution() {
    // write your code in Javascript
    //
    // you can access DOM Tree by using DOM Object Model:
    //    document.getElementsByTagName
    // or by using jQuery:
    //    $('some_tag')

    var myCollection = document.getElementsByTagName("table");
    var sp = [];
    var i;
    for (i = 0; i < mycollection.length; i++) {
        for (var j = 0; j < myCollection.length; j++) {
            sp[j] = myCollection[i].getElementsByTagName("td").length;
        }
    }

    function Max(sp) {
        var len = sp.length, max = -Infinity;
        while (len--) {
            if (sp) _
        }
    }
}
